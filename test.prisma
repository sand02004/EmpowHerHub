generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://localhost"
}

model User {
  id    String @id @default(uuid())
  email String @unique
}
